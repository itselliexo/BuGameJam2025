using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StunTrap : MonoBehaviour
{
   /* public float stunDuration = 2f; // Stun süresi

    private void OnTriggerEnter(Collider other)
    {
        // E?er obje "IStunnable" arayüzünü veya bir bile?eni içeriyorsa, onu stun yap
        IStunnable stunnable = other.GetComponent<IStunnable>();
        if (stunnable != null)
        {
            stunnable.Stun(stunDuration);
        }
    }
   */
}
